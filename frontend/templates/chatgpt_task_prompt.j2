### 🧠 Task ID: {{ task.task_id }}
**File:** {{ task.file }}
**Line Range:** {{ task.line_range }}
**Agent:** {{ task.agent_id }}

---

{{ task.description }}

```python
{{ task.code_block }}
```

---
**Response Format Requirement:**

Please respond *only* in **valid JSON** enclosed in a ```json code block, following this exact structure:

```json
{
  "task_id": "{{ task.task_id }}",
  "feedback": "APPROVED | REVISE | REJECTED",
  "details": "<Concise explanation if REVISE/REJECTED, otherwise empty string>",
  "raw_reply": "<Your full, original markdown response here, including code blocks if needed. Escape characters as required for a valid JSON string.>"
}
``` 