# Core Selenium for browser automation
selenium>=4.10.0

# Undetected chromedriver for bypassing bot detection
undetected-chromedriver>=3.5.0

# YAML parser (used by governance, find_todos)
PyYAML>=6.0

# Reddit API Wrapper (for RedditStrategy)
praw

# HTTP Requests library (for LinkedInStrategy)
requests

# Window management dependencies
pywin32>=305; platform_system == "Windows"
pyobjc-framework-Cocoa>=9.2; platform_system == "Darwin"
python-xlib>=0.33; platform_system == "Linux"
pyautogui>=0.9.54
pyperclip>=1.8.2

PyQt5==5.15.9
PyQt5-Qt5==5.15.2
PyQt5-sip==12.12.1
python-dateutil==2.8.2
pytz==2023.3
requests==2.31.0
pillow==10.0.0
numpy==1.24.3
pandas==2.0.2
opencv-python==4.8.0.74
loguru==0.7.0
GitPython==3.1.31

# Add Jinja2 for template rendering
jinja2 